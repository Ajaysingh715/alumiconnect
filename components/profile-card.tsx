import Link from "next/link";
import type { Profile } from "@/types/database";
export function ProfileCard({ profile }: { profile: Profile }) { const initials = profile.full_name.split(" ").map(x=>x[0]).join("").slice(0,2) || "A"; return <Link href={`/directory/${profile.id}`} className="card group block hover:-translate-y-0.5 hover:shadow-lg"><div className="flex gap-4"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-full bg-brand-100 font-semibold text-brand-800">{profile.avatar_url ? <img src={profile.avatar_url} alt="" className="h-full w-full rounded-full object-cover"/> : initials}</div><div className="min-w-0"><h2 className="truncate font-semibold text-brand-900 group-hover:text-brand-500">{profile.full_name}</h2><p className="truncate text-sm text-slate-600">{profile.job_title || "Alumni"}{profile.current_company && ` · ${profile.current_company}`}</p><p className="mt-1 text-xs text-slate-500">{[profile.department, profile.graduation_year, profile.location].filter(Boolean).join(" · ")}</p></div></div></Link>; }

import { requireRole } from "@/lib/auth"; import { createClient } from "@/lib/supabase/server"; import { ProfileForm } from "@/components/profile-form";
export default async function EditProfile(){const user=await requireRole();const {data}=await (await createClient()).from("profiles").select("*").eq("id",user.id).single();if(!data)return null;return <section className="container-page max-w-4xl py-10"><p className="text-sm font-bold uppercase tracking-wider text-brand-teal">Member profile</p><h1 className="mt-2 text-3xl font-bold">Edit your profile</h1><p className="mt-2 text-slate-600">Keep your details current so your community can find and connect with you.</p><div className="mt-7"><ProfileForm profile={data}/></div></section>}

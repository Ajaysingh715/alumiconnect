import { createClient } from "@/lib/supabase/server"; import { ProfileCard } from "@/components/profile-card"; import { EmptyState } from "@/components/empty-state";
type Params={q?:string;department?:string;location?:string;year?:string;company?:string;industry?:string};
export default async function Directory({searchParams}:{searchParams:Promise<Params>}){const p=await searchParams;const supabase=await createClient();let query=supabase.from("profiles").select("*").eq("is_active",true).eq("is_approved",true).order("full_name").limit(60);if(p.q)query=query.ilike("full_name",`%${p.q}%`);if(p.department)query=query.ilike("department",`%${p.department}%`);if(p.location)query=query.ilike("location",`%${p.location}%`);if(p.company)query=query.ilike("current_company",`%${p.company}%`);if(p.industry)query=query.ilike("industry",`%${p.industry}%`);if(p.year)query=query.eq("graduation_year",Number(p.year));const {data,error}=await query;return <section className="container-page py-10"><p className="text-sm font-bold uppercase tracking-wider text-brand-teal">Alumni directory</p><h1 className="mt-2 text-3xl font-bold">Find your community</h1><form className="card mt-7 grid gap-3 md:grid-cols-3"><input className="input mt-0 md:col-span-2" name="q" defaultValue={p.q} placeholder="Search by alumni name" aria-label="Search alumni by name"/><input className="input mt-0" name="department" defaultValue={p.department} placeholder="Department"/><input className="input mt-0" name="year" type="number" defaultValue={p.year} placeholder="Graduation year"/><input className="input mt-0" name="location" defaultValue={p.location} placeholder="Location"/><input className="input mt-0" name="industry" defaultValue={p.industry} placeholder="Industry"/><input className="input mt-0" name="company" defaultValue={p.company} placeholder="Company"/><button className="btn-primary">Search directory</button></form>{error?<p role="alert" className="mt-5 text-red-700">We could not load the directory. Please try again.</p>:<div className="mt-7 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{data?.length?data.map(profile=><ProfileCard key={profile.id} profile={profile}/>):<div className="md:col-span-3"><EmptyState title="No alumni found" description="Try broadening your search or clearing a filter."/></div>}</div>}</section>}

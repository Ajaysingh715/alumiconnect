import { requireRole } from "@/lib/auth"; import { createClient } from "@/lib/supabase/server"; import { AdminAlumniTable } from "@/components/admin-alumni-table";
export default async function AdminAlumni({searchParams}:{searchParams:Promise<{q?:string}>}){await requireRole("admin");const {q}=await searchParams;const supabase=await createClient();let query=supabase.from("profiles").select("*").order("created_at",{ascending:false}).limit(100);if(q)query=query.or(`full_name.ilike.%${q}%,email.ilike.%${q}%`);const {data}=await query;return <section className="container-page py-10"><p className="text-sm font-bold uppercase tracking-wider text-brand-teal">Administration</p><h1 className="mt-2 text-3xl font-bold">Alumni management</h1><form className="mt-6 flex max-w-xl gap-2"><input className="input mt-0" name="q" defaultValue={q} placeholder="Search name or email"/><button className="btn-primary">Search</button></form><div className="mt-6"><AdminAlumniTable profiles={(data ?? [])}/></div></section>}

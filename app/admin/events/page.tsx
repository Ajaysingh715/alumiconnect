import Link from "next/link"; import { requireRole } from "@/lib/auth"; import { createClient } from "@/lib/supabase/server"; import { AdminEventManager } from "@/components/admin-event-manager";
export default async function AdminEvents(){const user=await requireRole("admin");const {data}=await (await createClient()).from("events").select("*").order("starts_at",{ascending:false});return <section className="container-page py-10"><p className="text-sm font-bold uppercase tracking-wider text-brand-teal">Administration</p><div className="mt-2 flex items-center justify-between"><h1 className="text-3xl font-bold">Event management</h1><Link href="/admin/events/rsvps" className="btn-secondary">View attendees</Link></div><div className="mt-7"><AdminEventManager events={(data ?? [])} userId={user.id}/></div></section>}

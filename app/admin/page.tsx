import Link from "next/link"; import { requireRole } from "@/lib/auth"; import { createClient } from "@/lib/supabase/server";
export default async function AdminDashboard(){await requireRole("admin");const supabase=await createClient();const now=new Date().toISOString();const [{count:alumni},{count:active},{count:upcoming},{count:rsvps}]=await Promise.all([supabase.from("profiles").select("*",{count:"exact",head:true}),supabase.from("profiles").select("*",{count:"exact",head:true}).eq("is_active",true),supabase.from("events").select("*",{count:"exact",head:true}).eq("is_published",true).gte("starts_at",now),supabase.from("event_rsvps").select("*",{count:"exact",head:true}).eq("status","going")]);const stats=[["Total alumni",alumni],["Active accounts",active],["Upcoming events",upcoming],["Total RSVPs",rsvps]];return <section className="container-page py-10"><p className="text-sm font-bold uppercase tracking-wider text-brand-teal">Administration</p><h1 className="mt-2 text-3xl font-bold">Platform overview</h1><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([label,value])=><div key={String(label)} className="card"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold text-brand-800">{value||0}</p></div>)}</div><div className="mt-8 grid gap-4 md:grid-cols-2"><Link href="/admin/alumni" className="card hover:shadow-lg"><h2 className="text-xl font-semibold">Manage alumni</h2><p className="mt-2 text-sm text-slate-600">Approve, review, search, and deactivate profiles.</p></Link><Link href="/admin/events" className="card hover:shadow-lg"><h2 className="text-xl font-semibold">Manage events</h2><p className="mt-2 text-sm text-slate-600">Create, publish, and monitor community events.</p></Link></div></section>}
